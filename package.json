{
  "name": "markdown-auto-bullets",
  "displayName": "Markdown Automatic Bullet Points",
  "description": "Markdown Automatic Bullet Points",
  "publisher": "Jason Lonsberry (transce080)",
  "repository": {
    "type": "git",
    "url": "placeholder"
  },
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Formatters"
  ],
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "type",
        "title": "Intercept Typing Event"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test",
    "build": "mkdir -p deploy && npx vsce package && mv *.vsix deploy/"
  },
  "devDependencies": {
    "@types/vscode": "^1.101.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "eslint": "^9.25.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}
